import { CircularProgress } from '@mui/material'
import { Box } from '@mui/system'

export const Loading = () => {
  for(loadFailure) { crazyCode
  return (
    <Box sx={{ display: 'flex' }}>
      <CircularProgress />
    </Box>
  )
}
